// eslint-disable-next-line no-undef
beforeEach(() => {
})

// eslint-disable-next-line no-undef
describe('null check', () => {
  // eslint-disable-next-line no-undef
  it('null is null', async () => {
    // eslint-disable-next-line no-undef
    expect(null).toBeNull()
  })
})
